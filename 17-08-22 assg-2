#include<stdio.h>
int main (){
	int n;
	scanf("%d", &n);
	int i,a,b;
	for (i=1;i<n;i++){
		scanf("%d", &a);
		if(i!=1){
			if(a!=b+1){
				printf("%d", b+1);
				return 0;
				
			}
		}
		b=a;
	}
	printf("No Missing Numbers");
	return 0;
	
}
